import 'package:flutter/material.dart';

class ClassifyDrawer extends StatelessWidget{
  @override
    Widget build(BuildContext context) {
      return new Drawer(
        child: new Text("ClassifyDrawer"),
      );
    }
}